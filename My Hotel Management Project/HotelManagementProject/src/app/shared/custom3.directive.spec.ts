import { Custom3Directive } from './custom3.directive';

describe('Custom3Directive', () => {
  it('should create an instance', () => {
    const directive = new Custom3Directive();
    expect(directive).toBeTruthy();
  });
});
